language: java
jdk: [openjdk8]
script: 
  - ./gradlew spotlessApply
  - ./.ci/ci.sh
before_cache: [rm -f $HOME/.gradle/caches/modules-2/modules-2.lock]
cache:
  directories: [$HOME/.gradle/caches/, $HOME/.gradle/wrapper/, matlabcontrol/build/jdk]
branches:
  only: [diffplug/matconsolectl-23-GRADLE_INCREMENTAL_COMPILATION]
