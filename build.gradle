allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.fork = true
    }


    repositories {
        jcenter()
    }

    buildscript {
        repositories {
            jcenter()
        }
    }
}