allprojects {
    tasks.withType(JavaCompile).configureEach {
        options.incremental = true
    }


    repositories {
        jcenter()
    }

    buildscript {
        repositories {
            jcenter()
        }
    }
}